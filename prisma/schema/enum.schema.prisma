
enum UserRole {
    TOURIST
    HOST
    ADMIN
    SUPER_ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

enum Gender {
    MALE
    FEMALE
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum TravelType {
  SOLO
  FRIENDS
  FAMILY
}